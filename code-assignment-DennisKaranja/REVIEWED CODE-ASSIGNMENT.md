Please provide review-comment for the code below:

```

@Component			//Add Component Role. Can be null, int, float...charatcer types.
public class MyAction {
    public boolean debug = true;
	
    @Autowired
    public DataSource ds;	//A factory for connections to the physical data source that this DataSource object represents. 
	                       //An alternative to the DriverManager facility, a DataSource object is the preferred means of getting a connection.

	
    //The root interface in the COLLECTION hierarchy. A COLLECTION represents a group of objects, known as its elements.
    public Collection getCustomers(String firstName, String lastName, String address, String zipCode, String city) throws SQLException {
	
	//A connection (session) with a specific database. SQL statements are executed and results are returned within the context of a connection.
        Connection conn = ds.getConnection();
	//SQL Statement for obtaining all information from Customers table in the database.
        String query = new String("SELECT * FROM customers where 1=1");
		
	//Check condition of the result from the Query whether the firstName is not blank, then extract the rest of the details from the database.
        if (firstName != null) {
            query = query + " and first_name = '" + firstName + "'";
        }
        if (firstName != null) {
            query = query + " and last_name = '" + firstName + "'";
        }
        if (firstName != null) {
            query = query + " and address = '" + address + "'";
        }
        if (firstName != null) {
            query = query + " and zip_code = '" + zipCode + "'";
        }
        if (firstName != null) {
            query = query + " and city = '" + city + "'";
        }
		
	//The object used for executing a static SQL statement and returning the results it produces.
        Statement stmt = conn.createStatement();
		
	//A table of data representing a database result set, which is usually generated by executing a statement that queries the database.
        ResultSet rs = stmt.executeQuery(query);
		
	//An ordered collection (also known as a sequence). The user of this interface has precise control over where in the list each element is inserted. The //user can access elements by their integer index (position in the list), and search for elements in the list.
	
        List customers = new ArrayList();	//The ArrayList constructs an empty list with an initial capacity of ten.
		
	//Iterate through the list of the objects obtain from the ResultSet of the query response
        while (rs.next()) {
		
	    //Class Object is the root of the class hierarchy. Every class has Object as a superclass. All objects, including arrays, implement the methods of this class.
            Object[] objects = new Object[] { rs.getString(1), rs.getString(2) };
            if (debug) print(objects, 4);
			
	    //Appends the specified element to the end of this list (optional operation).
            customers.add(new Customer(rs.getString(1), rs.getString(2), rs.getString(3), rs.getString(4), rs.getString(5)));
        }
	//Returns a list of the collected data from the database table.
        return customers;
    }

    //Iterates through the printable objects obtained from the collection list.
    public void print(Object[] s, int indent) {
        for (int i=0; i<=indent; i++) System.out.print(' ');
        printUpper(s);
    }

    //This method prints to Upper Case the list of Strings obtained from the collection.
    public static void printUpper(Object [] words){
        int i = 0;
        try {
            while (true){
                if (words[i].getClass() == String.class) {
                    String so = (String)words[i];;
                    so = so.toUpperCase();
                    System.out.println(so);
                }
                i++;
            }
        } catch (IndexOutOfBoundsException e) {
            //iteration complete
        }
    }
}
```
